{
  "name": "api-gateway",
  "version": "1.0.0",
  "scripts": {
    "dev": "serverless offline",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless-offline": "^14.3.2"
  }
}
